---
id: T6.1-verificacao-preveniu-deploy
tipo: protocolo
estado: homologado
data: 2026-05-15
hipotese: "A etapa de auditoria-verificacao (T6.1a) antes de qualquer deployment detecta quando um risco_ativo já foi resolvido em produção, prevenindo deploy desnecessário"
resultado: validado
---

## DESCOBERTA

T6.1a revelou que fn_handle_new_user v2 já estava deployada em produção.
O risco_ativo documentado em profiles-001 (severidade alta) estava RESOLVIDO
antes do ciclo T6.1 ser iniciado.

Consequências concretas:
- T6.1b (migration) foi cancelada — estado correto preservado
- 0 profiles com NULL tenant_id (nenhum usuário afetado)
- fn_custom_access_token_hook confirmada operacional (cadeia JWT → RLS funcionando)
- Schema real de profiles documentado pela primeira vez (endereco, preferencias, tags)
- Discovery: 2 tenants em produção (1 esperado) — requer investigação

LIÇÃO: Snapshots de auditoria-inicial refletem estado CONHECIDO na época de criação,
não estado verificado. Sempre executar verificação antes de deploy baseado em
risco_ativo de snapshot tipo auditoria-inicial.

## IMPACTO ARQUITETURAL

Padrão "auditoria-verificacao antes de migration" confirmado como obrigatório
para ciclos baseados em risco_ativo de snapshots auditoria-inicial.
k-db-tabelas-core.md atualizado (v5.3 → v5.4) com schema real de profiles.
profiles-002 documenta estado verificado e risco anterior resolvido.
Contagem Sprint 5C: 2/30 ciclos reais.

## REFERÊNCIAS

telemetria: telemetria-20260515-T6.1a-profiles.md
commit: a registrar neste ciclo
modulos_afetados: r-recuperacao-contextual (profiles-002), k-db-tabelas-core (v5.4), k-sys-registry-dominios
snapshot_ref: profiles-002

## CAOS-CORE

Padrão promotável: SIM — "verificação antes de deployment para risco_ativo de auditoria-inicial".
Enunciado universal: "Execute verify-before-deploy cycle para qualquer risco_ativo
documentado em snapshot de tipo auditoria-inicial. O estado real pode diferir do estado
arquitetural inferido na época do snapshot."
Condição: aguardar ocorrência em segundo projeto para confirmar universalidade.
